<header class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <nav class="nav navbar-nav navbar-left">
      <%= link_to "た お や か", root_path %>
    </nav>
    <nav>
      <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li><%= link_to "My page", user_path(current_user.id), class:"glyphicon glyphicon-leaf" %></li>
        <li><%= link_to "Users", users_path, class:"glyphicon glyphicon-user" %></li>
        <li><%= link_to "Diary", tweets_path, class:"glyphicon glyphicon-align-justify" %></li>
        <li><%= link_to "Log out", destroy_user_session_path, method: :delete, class:"glyphicon glyphicon-log-out" %></li>
      <% else %>
        <li><%= link_to "Log in", new_user_session_path, class:"glyphicon glyphicon-log-in" %></li>
        <li><%= link_to "新規登録", new_user_registration_path, class:"glyphicon glyphicon-pencil" %></li>
      <% end %>
    </ul>
    </nav>
  </div>
</header>
